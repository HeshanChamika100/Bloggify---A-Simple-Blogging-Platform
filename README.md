## Setup

1. Install dependencies:

```bash
npm install
```

2. Create a `.env.local` file in this folder:

```env
NEXT_PUBLIC_API_URL=http://localhost:5000
```

3. Start the app:

```bash
npm run dev
```

